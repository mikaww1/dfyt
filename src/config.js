// Feature flag system for building different versions of the extension
// HIDE_ADS_ENABLED: false = store-safe version (no ad hiding)
// HIDE_ADS_ENABLED: true = full version (with ad hiding)
window.DFYT_CONFIG = {
  HIDE_ADS_ENABLED: true
};
